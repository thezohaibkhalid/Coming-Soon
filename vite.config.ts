import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Coming-Soon/', // Set this to your repository name
  build: {
    outDir: 'dist', // The folder where your build files will be stored
  },
});
